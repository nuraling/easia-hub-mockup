run = "npm start"
entrypoint = "src/main.jsx"

[nix]
channel = "stable-24_05"
packages = ["nodejs-20_x"]

[deployment]
build = ["sh", "-c", "npm install && npm run build"]
run = ["sh", "-c", "npm start"]
